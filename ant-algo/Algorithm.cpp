#include "Algorithm.h"

namespace Algo {
	// Считает вероятность перехода from ---> to
	void findNext(Node& node) noexcept {
	
	};

	// Обновляет количество феромонов на дороге road
	void updatePheromone(Path& road) noexcept {

	};

	// Находит длину пути от Node(х1, у1) до Node(х2, у2)
	double calcLength(double x1, double y1, double x2, double y2) noexcept {
		return 0;
	};

	void reset() {

	};

	void start() {

	};
}